x-logging: &default-logging
  driver: json-file
  options:
    max-size: "50m"
    max-file: "7"

x-healthcheck: &health_http
  interval: 30s
  timeout: 10s
  retries: 5
  start_period: 30s

services:
  tommanager-postgres:
    image: postgres:15
    container_name: tommanager-postgres
    hostname: tommanager-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_INITDB_ARGS=--data-checksums
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging: *default-logging

  tommanager-backend:
    image: ${REGISTRY_URL:-registry.matthias-bouloc.fr}/${IMAGE_NAME:-tommanager}/backend:${TAG:-latest}
    container_name: tommanager-backend
    hostname: tommanager-backend
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001
      # Defense en profondeur (cf docs/features/ParisTimezone) : UTC explicite,
      # jamais Paris, pour ne jamais masquer un code qui redependrait par erreur
      # du fuseau ambiant du serveur.
      - TZ=UTC
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@tommanager-postgres:5432/${POSTGRES_DB}?schema=public&connection_limit=20&pool_timeout=30
      - SESSION_SECRET=${SESSION_SECRET}
      - CORS_ORIGIN=${CORS_ORIGIN}
      # 2 hops : Traefik puis nginx (frontend) ajoutent chacun leur maillon a
      # X-Forwarded-For. Cf backend/src/config/env.ts.
      - TRUST_PROXY=2
      - NODE_OPTIONS=--max-old-space-size=1024
      - DISCORD_CLIENT_ID=${DISCORD_CLIENT_ID}
      - DISCORD_CLIENT_SECRET=${DISCORD_CLIENT_SECRET}
      - DISCORD_GUILD_ID=${DISCORD_GUILD_ID}
      - DISCORD_REDIRECT_URI=${DISCORD_REDIRECT_URI}
      - DISCORD_ADMIN_ROLE_ID=${DISCORD_ADMIN_ROLE_ID}
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN}
      - BGG_API_TOKEN=${BGG_API_TOKEN}
    volumes:
      - backend-logs:/app/logs
    networks:
      - internal
    depends_on:
      tommanager-postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3001/health"]
      <<: *health_http
    logging: *default-logging

  tommanager-frontend:
    image: ${REGISTRY_URL:-registry.matthias-bouloc.fr}/${IMAGE_NAME:-tommanager}/frontend:${TAG:-latest}
    container_name: tommanager-frontend
    hostname: tommanager-frontend
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - internal
      - proxy
    depends_on:
      tommanager-backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:80"]
      <<: *health_http
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tommanager-web.rule=Host(`tommanager.matthias-bouloc.fr`)"
      - "traefik.http.routers.tommanager-web.entrypoints=https"
      - "traefik.http.routers.tommanager-web.tls.certresolver=cloudflare"
      - "traefik.http.services.tommanager-web.loadbalancer.server.port=80"
      - "traefik.docker.network=proxy"
    logging: *default-logging

  tommanager-discord-bot:
    image: ${REGISTRY_URL:-registry.matthias-bouloc.fr}/${IMAGE_NAME:-tommanager}/discord-bot:${TAG:-latest}
    container_name: tommanager-discord-bot
    hostname: tommanager-discord-bot
    restart: unless-stopped
    environment:
      - TZ=UTC
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@tommanager-postgres:5432/${POSTGRES_DB}?schema=public
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN}
      - DISCORD_GUILD_ID=${DISCORD_GUILD_ID}
      - DISCORD_ADMIN_ROLE_ID=${DISCORD_ADMIN_ROLE_ID}
    networks:
      - internal
    depends_on:
      tommanager-backend:
        condition: service_healthy
    logging: *default-logging

networks:
  internal:
    driver: bridge
    name: tommanager_internal
  proxy:
    external: true
    name: proxy

volumes:
  postgres_data:
  backend-logs:
